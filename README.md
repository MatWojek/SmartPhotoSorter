# SmartPhotoSorter
SmartPhotoSorter is an AI-based photo organization tool designed to automatically categorize and sort images into meaningful folders. Using this dataset, the AI learns to recognize categories (such as family, pets, holidays, landscapes) and then applies this knowledge to classify and sort new, unsorted images.
